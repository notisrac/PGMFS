/**
	FileType

	Helper enum for the types of files stored

	@author noti (https://github.com/notisrac)
	@version 1.1 03/01/16

	https://github.com/notisrac/PGMFS
*/
typedef enum FileType { HTML, XML, CSS, PNG, GIF, XSL, ICO, JS, JSON, UNKNOWN, ERROR, TXT } FileType;